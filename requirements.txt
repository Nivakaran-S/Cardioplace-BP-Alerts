# --- pipeline ---
numpy
pandas
scikit-learn
scipy
pyyaml
joblib
pyarrow
python-dotenv

# --- experiment tracking ---
mlflow
dagshub

# --- serving (app.py) ---
# The Space runs the Gradio SDK, which installs gradio itself from the
# sdk_version in README.md; it is pinned here too so a local run matches.
gradio==5.36.2
fastapi
uvicorn[standard]
jinja2
pydantic
python-multipart
