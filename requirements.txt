# --- pipeline ---
# Pinned to the versions that produced final_model/model.pkl. Unpinned, the Space
# resolved scikit-learn 1.7.2 against a 1.8.0 pickle and sklearn warned that this
# "might lead to breaking code or invalid results" -- not a risk worth carrying on
# a clinical model. Re-pin these whenever the bundle is retrained.
numpy==2.4.2
pandas==2.3.3
scikit-learn==1.8.0
scipy==1.16.0
joblib==1.5.3
pyyaml
pyarrow
python-dotenv

# --- experiment tracking ---
mlflow
dagshub

# --- serving (app.py) ---
# The Space runs the Gradio SDK, which installs gradio itself from the
# sdk_version in README.md; it is pinned here too so a local run matches.
gradio==5.36.2
fastapi
uvicorn[standard]
jinja2
pydantic
python-multipart
